version: "3"
services:
  bot:
    build: .
    ports:
      - "9221:9221"
    container_name: skadi
    restart: on-failure
    volumes:
      - .:/home/HoshinoBot/HoshinoBot
    networks:
      - bot_net
networks:
  bot_net:
    driver: bridge
